pub mod en;
pub struct I18n<'a> {
    pub music:              &'a str,
    pub video:              &'a str,
    pub image:              &'a str,
    pub check_music_folder: &'a str,
    pub choose_a_folder:    &'a str,
    pub open:               &'a str,
    pub cancel:             &'a str,
    pub select:             &'a str
}